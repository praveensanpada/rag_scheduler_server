# ===== Global defaults (can be overridden per job) =====
# Big timeout so slow endpoints (10+ minutes) complete.
# This is the READ timeout (connect is kept short).
TIMEOUT_SECONDS=1200        # 20 minutes

RETRY_MAX=3                 # total retries for transient failures
RETRY_BASE_MS=1200          # base backoff (ms)

# ===== Logging =====
LOG_DIR=./logs
LOG_LEVEL=INFO              # DEBUG | INFO | WARNING | ERROR

# ===== Run behavior =====
RUN_ONCE=0                  # 1 = run all jobs once and exit
CONFIG_FILE=./config.yaml
